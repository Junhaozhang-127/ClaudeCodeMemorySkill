# Claude Code Memory Skill

## Skill 目标

当用户在 Claude Code 中进行多轮项目开发、Bug 修复、需求分析或测试复盘时，本 Skill 负责将对话沉淀为可检索的结构化 Markdown 记忆库，并在新会话中根据用户输入自动检索并注入相关历史上下文。

Phase 2 增强：支持基于规则的结构化摘要、关键决策抽取、待办事项提取和多字段加权检索评分。

## 适用场景

当用户出现以下意图时，应优先使用本 Skill：

- 继续之前的项目讨论
- 查找历史 Bug 修复记录
- 复用之前的提示词、方案或任务清单
- 追踪某个项目的需求变化
- 在新会话中恢复之前 Claude Code 的工作上下文

## 何时写入记忆

在以下时机触发记忆写入：

- 会话自然结束或用户主动要求保存
- 完成一个重要讨论阶段（如方案确定、Bug 修复完成）
- 用户明确说"记住这个"或"保存这条记忆"
- Hook 事件：PostConversation、Stop、或合适的 PostToolUse 事件

写入命令：

```bash
python scripts/summarize_session.py --topic "<对话主题>" --text "<本轮对话内容>"
```

## 何时检索记忆

在新会话开始或用户提出新问题时，应首先检索相关记忆：

```bash
python scripts/retrieve_memory.py --query "<用户当前输入>" --top-k 5
```

## 写入记忆格式

每条记忆现在包含结构化字段：

```markdown
# 主题名称

> 更新时间：YYYY-MM-DD HH:MM:SS

## 摘要
结构化摘要（规则抽取，不超过 500 字）

## 关键词
关键词1, 关键词2, 关键词3

## 关键决策
- 决策1
- 决策2
（无决策时显示"无明确关键决策。"）

## 待办事项
- 待办1
- 待办2
（无待办时显示"无明确待办事项。"）

## 原始对话摘录
对话原文（低优先级补充）
```

## 检索注入规则

将检索结果注入 Claude Code 上下文时，遵循**严格优先级**：

1. **首先注入摘要**：每条相关记忆的摘要最先展示
2. **其次注入关键决策**：从记忆中提取的决策列表（最多 3 条）
3. **然后注入待办事项**：从记忆中提取的待办列表（最多 3 条）
4. **最后补充原始内容**：仅在前三项总长度未超过限制时附加

### 格式示例

```markdown
# Claude Code Memory Context

## 相关记忆 1: 记忆系统架构设计

文件：memory/topics/...
相关分：25

**摘要**：讨论了记忆系统的可插拔架构设计...
**关键决策**：采用 jieba 分词；使用 BaseSummarizer 抽象基类
**待办事项**：补充单元测试；优化检索评分
**内容**：详细对话内容...（仅在空间充裕时展示）
```

## 输出限制

- **不要**把全部历史原文无脑注入上下文
- **不要**在无匹配时强行编造历史记忆
- **不要**忽略"无明确关键决策"或"无明确待办事项"占位文本的含义
- **优先注入**：摘要 → 关键决策 → 待办事项 → 原始内容（降序优先级）
- 对多个相关记忆，只选择最相关的 3 到 5 条
- 每条记忆最大输出 1200 字符
- 如果记忆与当前问题冲突，应在回答中说明"历史记录可能过期"

## 失败降级策略

| 异常情况 | 处理方式 |
|----------|----------|
| `index.json` 损坏或不存在 | `load_index()` 自动返回空字典，不崩溃 |
| 检索无匹配结果 | 返回"未检索到相关历史记忆。"，正常回答用户 |
| Markdown 文件被手动删除 | 检索时跳过不存在的文件，可用 `update_index.py` 重建 |
| jieba 未安装 | 自动回退到正则规则法提取关键词 |
| Python 脚本执行失败 | 不阻塞 Claude Code 正常响应，静默降级 |
| 决策/待办抽取为空 | 显示"无明确关键决策。"或"无明确待办事项。"，不虚构内容 |
| 旧格式 Markdown 文件 | `rebuild_index()` 自动兼容，无 decisions/todos 时使用空列表 |

## 安全注意事项

- 记忆文件存储在本地 `memory/` 目录，不会上传到任何远程服务
- 敏感信息（API Key、密码等）不应写入记忆
- `index.json` 使用原子写入（临时文件 + `os.replace()`），防止写入中断导致索引损坏
- 索引重建只扫描真实记忆 Markdown 文件，自动跳过 README.md、.gitkeep 和隐藏文件
- 摘要器为本地规则实现，不发送数据到外部 API

## 不应该做什么

- **不要**在每次对话微小时都触发记忆写入，只在阶段性成果时保存
- **不要**把 `memory/topics/README.md` 当成记忆文件索引
- **不要**在 Hook 中直接调用 Python 而不检查返回值
- **不要**假设检索结果一定准确——这是关键词匹配，不是语义搜索
- **不要**用记忆完全替代用户输入——记忆只是辅助上下文，用户当前指令优先级最高
- **不要**在对话中反复提醒用户"我有你的历史记忆"，只在相关时自然引用
- **不要**在无法抽取决策/待办时强行编造——使用占位文本或空列表
