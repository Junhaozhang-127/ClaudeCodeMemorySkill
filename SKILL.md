# Claude Code Memory Skill

## Skill 目标

当用户在 Claude Code 中进行多轮项目开发、Bug 修复、需求分析或测试复盘时，本 Skill 负责将对话沉淀为可检索的 Markdown 记忆库，并在新会话中根据用户输入自动检索并注入相关历史上下文。

## 适用场景

当用户出现以下意图时，应优先使用本 Skill：

- 继续之前的项目讨论
- 查找历史 Bug 修复记录
- 复用之前的提示词、方案或任务清单
- 追踪某个项目的需求变化
- 在新会话中恢复之前 Claude Code 的工作上下文

## 何时写入记忆

在以下时机触发记忆写入：

- 会话自然结束或用户主动要求保存
- 完成一个重要讨论阶段（如方案确定、Bug 修复完成）
- 用户明确说"记住这个"或"保存这条记忆"
- Hook 事件：PostConversation、Stop、或合适的 PostToolUse 事件

写入命令：

```bash
python scripts/summarize_session.py --topic "<对话主题>" --text "<本轮对话内容>"
```

或从文件读取：

```bash
python scripts/summarize_session.py --topic "<主题>" --file "<对话文本文件路径>"
```

## 何时检索记忆

在新会话开始或用户提出新问题时，应首先检索相关记忆：

```bash
python scripts/retrieve_memory.py --query "<用户当前输入>" --top-k 5
```

检索时机：

- 用户提出与技术、项目、历史相关的问题
- 用户提到"之前我们讨论过"或"上次的那个"
- 用户在新会话中首次提出涉及项目上下文的问题
- Hook 事件：PrePrompt、NewConversation 等

## 写入记忆格式

每条记忆应通过 `save_memory()` 生成以下结构：

```markdown
# 主题名称

> 更新时间：YYYY-MM-DD HH:MM:SS

## 摘要
简要概述本轮对话的核心内容和结论。

## 关键词
关键词1, 关键词2, 关键词3

## 关键决策
- 决策1
- 决策2

## 待办事项
- 待办1
- 待办2

## 原始对话摘录
对话原文（必要时保留关键片段）
```

> 注：MVP 版本中"关键决策"和"待办事项"字段暂未自动抽取，后续将接入 LLM 生成。

## 检索注入规则

将检索结果注入 Claude Code 上下文时，遵循以下规则：

1. **优先注入摘要和关键决策**，而非全文对话记录
2. **仅选择最相关的 3 到 5 条**记忆，避免上下文过载
3. 注入格式：

```markdown
# Claude Code Memory Context

## 相关记忆 1: <主题>
文件：<路径>
相关分：<分数>

<记忆内容摘要>
```

4. 如果检索到相关记忆，应先阅读其中摘要、关键决策与待办，再结合当前问题回答
5. 如果检索不到记忆，正常回答，不要强行编造历史

## 输出限制

- **不要**把全部历史原文无脑注入上下文
- **不要**在无匹配时强行编造历史记忆
- **不要**忽略记忆中的"历史记录可能过期"标记
- 优先注入摘要、关键决策、待办事项
- 对多个相关记忆，只选择最相关的 3 到 5 条
- 如果记忆与当前问题冲突，应在回答中说明"历史记录可能过期"

## 失败降级策略

| 异常情况 | 处理方式 |
|----------|----------|
| `index.json` 损坏或不存在 | `load_index()` 自动返回空字典，不崩溃 |
| 检索无匹配结果 | 返回"未检索到相关历史记忆"，正常回答用户 |
| Markdown 文件被手动删除 | 检索时跳过不存在的文件，可用 `update_index.py` 重建索引 |
| Python 脚本执行失败 | 不阻塞 Claude Code 正常响应，静默降级 |
| Hook 未配置 | Skill 仍可通过手动 CLI 命令使用 |

## 安全注意事项

- 记忆文件存储在本地 `memory/` 目录，不会上传到任何远程服务
- 敏感信息（API Key、密码等）不应写入记忆。建议用户在对话中避免暴露敏感信息，或手动编辑记忆文件删除
- `index.json` 使用原子写入（先写临时文件，成功后再替换），防止写入中断导致索引损坏
- 索引重建只扫描 Markdown 文件，自动跳过 README.md、.gitkeep 和隐藏文件

## 不应该做什么

- **不要**在每次对话微小时都触发记忆写入，只在阶段性成果时保存
- **不要**把 `memory/topics/README.md` 当成记忆文件索引
- **不要**在 Hook 中直接调用 Python 而不检查返回值
- **不要**假设检索结果一定准确，应向用户说明这是基于关键词匹配的历史记录
- **不要**用记忆完全替代用户输入——记忆只是辅助上下文，用户当前指令优先级最高
- **不要**在对话中反复提醒用户"我有你的历史记忆"，只在相关时自然引用
